<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glacier.modules.sys.mapper.RoleDeptMapper">
    <resultMap id="BaseResultMap" type="com.glacier.modules.sys.entity.pojo.RoleDept">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="role_id" jdbcType="VARCHAR" property="roleId"/>
        <result column="dept_id" jdbcType="VARCHAR" property="deptId"/>
    </resultMap>

    <sql id="base_columns">
        a.id, a.role_id, a.dept_id
    </sql>

    <sql id="join_columns">
    </sql>

    <sql id="joins">
    </sql>

    <delete id="deleteByRoleId" parameterType="java.lang.String">
        delete from sys_role_dept a
        where a.role_id = #{roleId}
    </delete>

    <delete id="deleteByDeptId" parameterType="java.lang.String">
        delete from sys_role_dept a
        where a.dept_id = #{deptId}
    </delete>

    <delete id="deleteByDeptIds">
        delete from sys_role_dept a
        <where>
            <choose>
                <when test="list != null and list.size() > 0">
                    and a.dept_id in
                    <foreach collection="list" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </when>
                <otherwise>
                    0=1
                </otherwise>
            </choose>
        </where>
    </delete>

    <insert id="insert" parameterType="com.glacier.modules.sys.entity.pojo.RoleDept">
        insert into sys_role_dept (id, role_id, dept_id)
        values (#{id}, #{roleId}, #{deptId})
    </insert>
</mapper>
