bootJar {
    baseName("eboot-dfs")
}

ext {
}

dependencies {
    // 本地依赖
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation("${dependency.minio}")
    implementation("${dependency.commonsIo}")
    implementation("${dependency.commonsLang3}")
}
